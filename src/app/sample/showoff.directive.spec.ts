import { ShowoffDirective } from './showoff.directive';

describe('ShowoffDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowoffDirective();
    expect(directive).toBeTruthy();
  });
});
